name: WriteGuard evaluation

on:
  pull_request:
  push:
    branches: [master, main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  repository-history:
    name: Repository history safety
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: node scripts/scan-git-history-secrets.mjs

  local-evaluation:
    name: Local evaluation (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.9.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @closure/writeguard build
      - run: pnpm --filter @closure/writeguard-analyzer-openai build
      - run: pnpm --filter @closure/writeguard-generator build
      - run: pnpm typecheck
      - run: pnpm build
      - run: pnpm test:unit
      - run: pnpm validate:generated-artifacts
      - run: pnpm package:verify-generator
      - run: pnpm verify:core-openai-boundary
      - run: pnpm verify:generator-boundary
      - run: pnpm security:scan
      - run: pnpm docs:scan
      - run: pnpm evaluate:local
      - uses: actions/upload-artifact@v4
        if: ${{ always() && hashFiles('.writeguard/evaluation-*') != '' }}
        with:
          name: writeguard-evaluation-${{ matrix.os }}
          path: .writeguard/evaluation-*
          include-hidden-files: true
          if-no-files-found: error
